syntax = "proto3";

package domain;
option go_package = "marketplace-services/pkg/domain";

message Settlement {
    uint64 actualCost = 1;
    uint64 provider = 2;
    uint64 consumer = 3;
    uint64 broke = 4;
}